---
version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "3.12"
  apt_packages:
    - cmake
    - doxygen
    - graphviz

sphinx:
  configuration: "docs/conf.py"

python:
  install:
    - requirements: "docs/requirements.txt"
